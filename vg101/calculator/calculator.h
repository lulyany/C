#ifndef calculator_h
#define calculator_h
#include <stdio.h>
double evaluate(int operand1, char operator, int operand2) {
    switch operator
    case '+'
        return(operand1 + operand2);
    break;
    case '-'
        return(operand1 - operand2);
    break;
    case '*'
        return(operand1 * operand2);
    break;
    case '/'
        return(operand1 / operand2);
    break;
}
#endif /* calculator_h */
